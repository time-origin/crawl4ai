# .env.example
# 请将此文件复制为 .env 并填入您的实际配置。

# --- X.com 凭证 ---
X_USERNAME="your_x_username"
X_PASSWORD="your_x_password"

# --- 代理服务器 ---
# 如果不需要代理，请将此行注释掉或留空
# PROXY_SERVER="http://your_proxy_server:port"

# --- Kafka 配置 ---
KAFKA_BOOTSTRAP_SERVERS="localhost:9092"
KAFKA_TOPIC="x_com_scraped_data"
KAFKA_TASK_TOPIC="task_control_topic"

# --- 认证文件路径 (在Docker容器内) ---
# 通常在 Docker 运行时通过 -v 参数挂载，并在此处指定容器内的绝对路径
AUTH_JSON_PATH="/app/auth_state.json"

# --- [新增] 抓取失败时的最大重试次数 ---
# 定义在首次尝试抓取回复失败后，额外进行的重试次数。
# 值为 "1" 表示总共会尝试 1 (首次) + 1 (重试) = 2 次。
MAX_RETRIES_ON_FAILURE=1
